<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mts_health.allergyambassador">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Dashboard"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AllergenLawsPage"
            android:label="@string/allergen_laws_page_title" ></activity>
        <activity
            android:name=".CompareCountryLawsPage"
            android:label="@string/compare_countries_title" />
        <activity
            android:name=".PersonalReactionJournalPage"
            android:label="@string/personal_reaction_journal_title" />
        <activity android:name=".PhrasebookPage"
            android:label="@string/phrasebook_title"/>
    </application>

</manifest>
